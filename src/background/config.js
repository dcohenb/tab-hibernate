var ONE_SECOND = 1000,
    ONE_MINUTE = ONE_SECOND * 60,
    config = {};

// App
config.first_run = true;
config.auto_hibernate_after = 15; // in minutes
config.wake_up_on_focus = true;
config.transparent_favicon = 0.45;
config.tab_screenshot = false;
config.scan_tabs_interval = 10 * ONE_SECOND;